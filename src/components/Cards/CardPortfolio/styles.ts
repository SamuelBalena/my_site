import { styled } from "../../../styles";

export const Container = styled("div", {
  width: "100%",
  margin: "0 auto",

  aside: {
    height: 300,
    width: 500,
    backgroundColor: "$gray",
    borderRadius: 5,
  },
});
